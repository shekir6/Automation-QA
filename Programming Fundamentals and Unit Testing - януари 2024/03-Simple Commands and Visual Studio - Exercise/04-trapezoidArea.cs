int firstBase = int.Parse(Console.ReadLine());
int secondBase = int.Parse(Console.ReadLine());
int height = int.Parse(Console.ReadLine());

int sum = (firstBase + secondBase) / 2 * height;

Console.WriteLine(sum);